#include <iostream>
#include "HeartRates.h"
using namespace std;

HeartRates::HeartRates(string name, string surname, int day, int month, int year)
{
    setName(name);
    setSurname(surname);
    setDay(day);
    setMonth(month);
    setYear(year);
}
void HeartRates::setName(nam)
{
    name = nam;
}

